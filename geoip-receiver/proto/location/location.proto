syntax = "proto3";

package location;

option go_package = "geoip-receiver/proto/location";

message LocationPayload {
  string correlation_id = 1;
  string id             = 2;
  double lat            = 3;
  double lon            = 4;
  int64  timestamp      = 5;
}

message Ack {
  bool ok = 1;
}

service LocationReceiver {
  rpc Stream(stream LocationPayload) returns (stream Ack);
}
